# Railway конфигурация для проекта VeraliA
# Этот файл используется для настройки деплоя на Railway

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

