# Проверка Dockerfile для Parsing Server - КРИТИЧНО!

## ❌ Проблема повторяется:

Parsing Server снова запускает код Client Server, который требует `TELEGRAM_BOT_TOKEN`.

Это означает, что **Dockerfile Path НЕ был изменен** или **изменения не сохранились**.

---

## ✅ КРИТИЧЕСКАЯ ПРОВЕРКА:

### Проверьте настройки Parsing Server в Railway:

1. **Откройте Parsing Server (VeraliA)** в Railway
2. Перейдите в **Settings → Build**
3. **ПРОВЕРЬТЕ** поле **"Dockerfile Path"**

#### Должно быть:
```
Dockerfile Path: Dockerfile.parsing
```

#### НЕ должно быть:
```
Dockerfile Path: Dockerfile  ❌
```

---

## 🔧 Исправление:

### Шаг 1: Откройте настройки Build

1. **Parsing Server (VeraliA)** → **Settings** → **Build**
2. Найдите поле **"Dockerfile Path"**

### Шаг 2: Измените значение

1. **Удалите** текущее значение (`Dockerfile`)
2. **Введите** новое значение:
   ```
   Dockerfile.parsing
   ```
3. **БЕЗ** ведущего слэша `/`!
4. **БЕЗ** пробелов в начале и конце!

### Шаг 3: Сохраните

1. **Нажмите** кнопку **"Save"** или **галочку ✓**
2. Убедитесь, что изменения **сохранились**
3. Проверьте, что значение осталось `Dockerfile.parsing`

### Шаг 4: Проверьте Root Directory

1. Поле **"Root Directory"** должно быть **ПУСТЫМ**
2. Если там что-то есть → **удалите** значение

### Шаг 5: Redeploy

1. После сохранения Railway автоматически перезапустит сервис
2. Или нажмите **"Redeploy"** вручную

---

## ✅ Правильные настройки для Parsing Server:

```
Root Directory: (ПУСТО - корень проекта)
Dockerfile Path: Dockerfile.parsing
Builder: Dockerfile
```

---

## 🔍 Как проверить, что изменения сохранились:

1. После сохранения **обновите страницу** (F5)
2. Снова откройте **Settings → Build**
3. Проверьте, что **"Dockerfile Path"** = `Dockerfile.parsing`
4. Если вернулось к `Dockerfile` → изменения не сохранились

---

## ⚠️ Возможные проблемы:

### Проблема 1: Изменения не сохраняются

**Решение:**
- Убедитесь, что нажали **"Save"** или **галочку ✓**
- Проверьте, что нет ошибок валидации
- Попробуйте сохранить еще раз

### Проблема 2: После сохранения значение возвращается

**Решение:**
- Удалите переменную и создайте заново
- Или используйте **Raw Editor** в Variables

### Проблема 3: Railway не видит Dockerfile.parsing

**Решение:**
- Убедитесь, что файл `Dockerfile.parsing` существует в корне проекта
- Проверьте, что файл закоммичен в git
- Убедитесь, что Railway подключен к правильной ветке репозитория

---

## 📋 Пошаговая инструкция (повтор):

1. **Parsing Server (VeraliA)** → **Settings** → **Build**
2. **Dockerfile Path**: измените на `Dockerfile.parsing`
3. **Root Directory**: убедитесь, что ПУСТО
4. **Сохраните** (галочка ✓)
5. **Обновите страницу** и проверьте, что значение сохранилось
6. **Redeploy**

---

## ✅ После исправления:

Parsing Server должен:
- ✅ Использовать `Dockerfile.parsing`
- ✅ Копировать файлы из `parsing-server/`
- ✅ Запускать `parsing-server/app.py` (который НЕ требует TELEGRAM_BOT_TOKEN)
- ✅ Работать только с `DATABASE_URL`

---

## 🔍 Проверка в логах:

После деплоя в логах должно быть:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```

И **НЕ должно быть** ошибок про `TELEGRAM_BOT_TOKEN`.

---

## 🎯 Итого:

1. ✅ **Проверьте** Dockerfile Path = `Dockerfile.parsing`
2. ✅ **Сохраните** изменения
3. ✅ **Проверьте**, что изменения сохранились
4. ✅ **Redeploy**

Если проблема повторяется - проверьте, что файл `Dockerfile.parsing` существует в репозитории!


