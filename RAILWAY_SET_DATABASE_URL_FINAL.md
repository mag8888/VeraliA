# Установка DATABASE_URL для Parsing Server - Финальная инструкция

## ✅ Прогресс:

- ✅ Parsing Server использует правильный Dockerfile (`Dockerfile.parsing`)
- ❌ Переменная `DATABASE_URL` не установлена или пуста

---

## 🔧 Решение: Установите DATABASE_URL

### Шаг 1: Проверьте имя сервиса базы данных

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных (обычно "Postgres" или "PostgreSQL")
3. **Запомните точное имя** (с учетом регистра!)

### Шаг 2: Установите переменную DATABASE_URL

1. **Откройте Parsing Server (VeraliA)** в Railway
2. Перейдите в **Settings → Variables**
3. Нажмите **"+ New Variable"**
4. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте точное имя сервиса базы данных!

### Шаг 3: Если переменная уже существует

1. Найдите переменную `DATABASE_URL`
2. Нажмите на неё для редактирования
3. Убедитесь, что значение:
   - `${{Postgres.DATABASE_URL}}` (если база называется Postgres)
   - `${{PostgreSQL.DATABASE_URL}}` (если база называется PostgreSQL)
4. **Сохраните** (галочка ✓)

---

## 🔍 Альтернативный способ (если переменная не работает):

Если `${{Postgres.DATABASE_URL}}` не разрешается:

1. Откройте **сервис базы данных (Postgres)** → **Variables**
2. Найдите переменную `DATABASE_URL`
3. **Скопируйте** её значение (полный URL)
4. В **Parsing Server** → **Variables** установите:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение
5. **Сохраните**

---

## ✅ Правильные настройки Variables для Parsing Server:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

Или если база называется иначе:
```
DATABASE_URL=${{PostgreSQL.DATABASE_URL}}
```

---

## 📋 Пошаговая инструкция:

1. **Parsing Server (VeraliA)** → **Settings** → **Variables**
2. **+ New Variable** (или редактируйте существующую)
3. **Name**: `DATABASE_URL`
4. **Value**: `${{Postgres.DATABASE_URL}}` (или правильное имя)
5. **Сохраните** (галочка ✓)
6. **Redeploy** (или Railway перезапустит автоматически)

---

## ✅ После установки:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. В логах должно появиться:
   ```
   INFO: Database initialized
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```

---

## 🔍 Проверка:

После деплоя проверьте:
- ✅ В логах НЕ должно быть ошибок про `DATABASE_URL не установлен`
- ✅ Должно быть сообщение `Database initialized`
- ✅ Сервис должен запуститься успешно

---

## ⚠️ Важно:

- **Точное имя сервиса** (с учетом регистра!)
- **Без пробелов** в начале и конце значения
- **Правильный синтаксис**: `${{Имя_Сервиса.DATABASE_URL}}`

---

## 🎯 Итого:

1. ✅ Parsing Server использует `Dockerfile.parsing` - **ГОТОВО**
2. ❌ Нужно установить `DATABASE_URL` в Variables
3. ✅ Используйте `${{Postgres.DATABASE_URL}}` или правильное имя
4. ✅ Сохраните и Redeploy

После этого Parsing Server должен работать!


