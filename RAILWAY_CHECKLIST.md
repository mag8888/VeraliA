# Чеклист для деплоя на Railway

## ✅ Проверка перед деплоем

### Для Client Server:
- [ ] Root Directory установлен: `client-server` (без слэша `/`)
- [ ] Dockerfile существует в `client-server/Dockerfile`
- [ ] Переменные окружения настроены (TELEGRAM_BOT_TOKEN, DATABASE_URL, и т.д.)
- [ ] Деплой перезапущен после установки Root Directory

### Для Parsing Server:
- [ ] Root Directory установлен: `parsing-server` (без слэша `/`)
- [ ] Dockerfile существует в `parsing-server/Dockerfile`
- [ ] Переменные окружения настроены (DATABASE_URL)
- [ ] Деплой перезапущен после установки Root Directory

### Для PostgreSQL:
- [ ] Сервис создан через "Database" → "Add PostgreSQL"
- [ ] Root Directory НЕ установлен (не нужен для базы данных)

## 🔧 Если ошибка "Dockerfile does not exist"

### Шаг 1: Проверьте Root Directory
1. Откройте **Settings** сервиса
2. Найдите **Root Directory**
3. Убедитесь, что значение правильное:
   - Client Server: `client-server`
   - Parsing Server: `parsing-server`
4. **Убедитесь, что НЕТ слэша в начале!**
   - ❌ Неправильно: `/client-server`
   - ✅ Правильно: `client-server`

### Шаг 2: Сохраните и перезапустите
1. **Сохраните** изменения в Settings
2. Перейдите на вкладку **Deployments**
3. Нажмите на три точки (⋮) у последнего deployment
4. Выберите **"Redeploy"**
5. Или сделайте новый коммит в GitHub - Railway автоматически перезапустит

### Шаг 3: Проверьте настройки Build
1. В Settings найдите раздел **"Build"**
2. Убедитесь, что:
   - **Build Command** - пусто
   - **Dockerfile Path** - `Dockerfile` (или пусто)
   - **Root Directory** - правильное значение

### Шаг 4: Проверьте логи
1. Нажмите **"View logs"** на failed deployment
2. Проверьте, что Railway видит правильный путь
3. Ищите строки типа: "Building from Dockerfile..."

## 🚨 Если ничего не помогает

### Вариант 1: Пересоздать сервис
1. **Settings** → **Danger** → **Delete Service**
2. Создайте новый сервис из GitHub репозитория
3. **Сразу** установите Root Directory
4. Сохраните и дождитесь деплоя

### Вариант 2: Проверить GitHub
1. Убедитесь, что в GitHub репозитории файлы на месте:
   - `client-server/Dockerfile` существует
   - `parsing-server/Dockerfile` существует
2. Проверьте, что последний коммит загружен в GitHub

### Вариант 3: Использовать Nixpacks
Если Dockerfile не работает, Railway может автоматически использовать Nixpacks:
1. Удалите Root Directory (оставьте пустым)
2. Railway автоматически определит Python проект
3. Но лучше использовать Dockerfile для полного контроля

## 📝 Правильная структура на Railway

```
Railway Project: VeraliA
├── Postgres
│   └── Root Directory: (не установлен) ✅
│
├── Client-Server
│   ├── Root Directory: client-server ✅
│   ├── Dockerfile найден ✅
│   └── Variables настроены ✅
│
└── Parsing-Server
    ├── Root Directory: parsing-server ✅
    ├── Dockerfile найден ✅
    └── Variables настроены ✅
```

