# Исправление: Ошибка "Not Found" при открытии мини-приложения

## ❌ Проблема:

При нажатии на кнопку "Открыть мини-приложение" появляется ошибка "Not Found".

Это означает, что URL мини-приложения неправильный или недоступен.

---

## ✅ Решение:

### Шаг 1: Проверьте публичный URL Client Server

1. **Client Server** → **Settings** → **Networking**
2. Найдите **публичный URL** сервиса (например, `https://client-server-production.up.railway.app`)
3. **Скопируйте** этот URL

### Шаг 2: Обновите MINIAPP_URL в Variables

1. **Client Server** → **Settings** → **Variables**
2. Найдите переменную `MINIAPP_URL`
3. Обновите значение на:
   ```
   https://ваш-публичный-url.up.railway.app/miniapp
   ```
   
   Например:
   ```
   https://client-server-production.up.railway.app/miniapp
   ```
4. **Сохраните** (галочка ✓)
5. **Redeploy** Client Server

### Шаг 3: Проверьте, что URL доступен

1. Откройте в браузере: `https://ваш-публичный-url.up.railway.app/miniapp`
2. Должна открыться страница мини-приложения
3. Если ошибка "Not Found" → проверьте маршрут `/miniapp` в коде

---

## 🔍 Проверка:

### 1. Проверьте публичный URL

**Client Server** → **Settings** → **Networking**:
- Должен быть **публичный домен** (например, `client-server-production.up.railway.app`)
- Если нет → нажмите **"Generate Domain"** или **"Custom Domain"**

### 2. Проверьте Variables

**Client Server** → **Variables**:
```
MINIAPP_URL=https://ваш-публичный-url.up.railway.app/miniapp
```

**Важно:**
- URL должен начинаться с `https://` (не `http://`)
- URL должен заканчиваться на `/miniapp`
- URL должен быть публичным (доступным из интернета)

### 3. Проверьте маршрут в коде

В `client-server/app.py` должен быть маршрут:
```python
@app.get("/miniapp", response_class=HTMLResponse)
async def miniapp(request: Request):
    return templates.TemplateResponse("miniapp.html", {"request": request})
```

---

## 📋 Пошаговая инструкция:

1. **Client Server** → **Settings** → **Networking**
2. Найдите или создайте **публичный домен**
3. **Скопируйте** публичный URL (например, `https://client-server-production.up.railway.app`)
4. **Client Server** → **Variables**
5. Найдите `MINIAPP_URL`
6. Обновите значение: `https://ваш-публичный-url.up.railway.app/miniapp`
7. **Сохраните** и **Redeploy**

---

## ⚠️ Важно:

- **URL должен быть HTTPS** (Telegram требует HTTPS для WebApp)
- **URL должен быть публичным** (доступным из интернета)
- **URL должен заканчиваться на `/miniapp`**

---

## ✅ После исправления:

1. **Сохраните** изменения
2. **Redeploy** Client Server
3. Проверьте в браузере: `https://ваш-публичный-url.up.railway.app/miniapp`
4. Проверьте в Telegram: нажмите кнопку "Открыть мини-приложение"
5. Мини-приложение должно открыться

---

## 🎯 Итого:

1. ✅ Проверьте публичный URL Client Server
2. ✅ Обновите `MINIAPP_URL` в Variables
3. ✅ Убедитесь, что URL доступен
4. ✅ Redeploy и проверьте

После этого мини-приложение должно работать!

