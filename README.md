# Verali - Микросервисное решение для анализа Instagram

Проект состоит из двух микросервисов:
1. **Client Server** - Telegram бот и мини-приложение для отображения данных
2. **Parsing Server** - Сервер для анализа Instagram профилей и записи данных в базу

## Архитектура

```
┌─────────────────┐
│  Telegram Bot   │
│  (Client Server)│
└────────┬────────┘
         │
         ├──────────────┐
         │              │
         ▼              ▼
┌─────────────────┐  ┌─────────────────┐
│  Mini App       │  │  Parsing Server │
│  (Web Interface)│  │  (Image Parser) │
└─────────────────┘  └────────┬────────┘
                              │
                              ▼
                       ┌──────────────┐
                       │  PostgreSQL  │
                       │   Database   │
                       └──────────────┘
```

## Технологии

- **FastAPI** - веб-фреймворк для API
- **Python Telegram Bot** - библиотека для Telegram бота
- **PostgreSQL** - база данных
- **SQLAlchemy** - ORM для работы с БД
- **Tesseract OCR** - распознавание текста с изображений
- **OpenCV** - обработка изображений
- **Docker & Docker Compose** - контейнеризация

## Структура проекта

```
VeraliA/
├── client-server/          # Клиентский сервер
│   ├── app.py             # Основное приложение (бот + API)
│   ├── templates/         # HTML шаблоны
│   │   └── miniapp.html   # Мини-приложение
│   ├── requirements.txt   # Зависимости
│   └── Dockerfile         # Docker образ
├── parsing-server/        # Сервер парсинга
│   ├── app.py             # API сервер
│   ├── database.py        # Модели БД
│   ├── image_parser.py    # Парсер изображений
│   ├── requirements.txt   # Зависимости
│   └── Dockerfile         # Docker образ
├── docker-compose.yml     # Оркестрация сервисов
├── README.md              # Документация
├── RAILWAY_DEPLOY.md      # Инструкция по деплою на Railway
├── RAILWAY_QUICKSTART.md  # Быстрый старт Railway
└── DATA_FORMAT.md         # Формат данных
```

## Деплой на Railway

Для быстрого деплоя на Railway см. [RAILWAY_QUICKSTART.md](./RAILWAY_QUICKSTART.md)

Полная инструкция: [RAILWAY_DEPLOY.md](./RAILWAY_DEPLOY.md)

## Установка и запуск (локально)

### Предварительные требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Шаги установки

1. **Клонируйте репозиторий** (если используется Git)

2. **Создайте файл `.env`** в корне проекта:
```bash
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
MINIAPP_URL=https://your-domain.com/miniapp  # Для продакшена используйте HTTPS URL
```

**Важно:** Для Telegram WebApp требуется HTTPS URL. Для локальной разработки можно использовать:
- [ngrok](https://ngrok.com/) для создания туннеля
- Или другой сервис туннелирования

3. **Запустите проект**:
```bash
docker-compose up -d
```

4. **Проверьте статус сервисов**:
```bash
docker-compose ps
```

5. **Просмотрите логи**:
```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f client-server
docker-compose logs -f parsing-server
```

## Использование

### Telegram бот

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите "📈 Анализ Instagram"
4. Отправьте username пользователя (например: `username` или `@username`)
5. Отправьте скриншот статистики профиля Instagram

### Мини-приложение

1. В боте нажмите "📊 Открыть мини-приложение"
2. Введите username пользователя
3. Просмотрите статистику

## API Endpoints

### Client Server (порт 8000)

- `GET /` - Главная страница
- `GET /miniapp` - Мини-приложение
- `GET /api/data/{username}` - Получить данные пользователя
- `GET /api/users` - Получить список всех пользователей

### Parsing Server (порт 8001)

- `GET /` - Статус сервера
- `GET /health` - Health check
- `POST /api/analyze` - Анализ скриншота Instagram
  - Параметры:
    - `username` (form-data): Username пользователя
    - `screenshot` (file): Скриншот статистики
- `GET /api/data/{username}` - Получить данные пользователя
- `GET /api/users` - Получить список всех пользователей
- `DELETE /api/data/{username}` - Удалить данные пользователя

## Формат данных в базе

```python
{
    "username": "string",           # Username Instagram
    "followers": int,               # Количество подписчиков
    "following": int,               # Количество подписок
    "posts_count": int,             # Количество публикаций
    "bio": "string",                # Биография профиля
    "engagement_rate": float,       # Engagement rate (0-1)
    "screenshot_path": "string",    # Путь к сохраненному скриншоту
    "analyzed_at": "datetime",      # Дата и время анализа
    "created_at": "datetime",       # Дата создания записи
    "updated_at": "datetime"        # Дата последнего обновления
}
```

## Пример скриншота

Скриншот должен содержать:
- Количество подписчиков
- Количество подписок
- Количество публикаций
- Биографию профиля (опционально)

## Разработка

### Локальная разработка без Docker

1. **Установите зависимости**:
```bash
cd client-server
pip install -r requirements.txt

cd ../parsing-server
pip install -r requirements.txt
```

2. **Настройте базу данных PostgreSQL** локально

3. **Запустите сервисы**:
```bash
# Client Server
cd client-server
python app.py

# Parsing Server (в другом терминале)
cd parsing-server
python app.py
```

### Остановка сервисов

```bash
docker-compose down
```

### Очистка данных

```bash
# Остановить и удалить контейнеры и volumes
docker-compose down -v
```

## Troubleshooting

### Проблемы с распознаванием текста

- Убедитесь, что скриншот четкий и хорошо читаемый
- Проверьте, что на скриншоте видны числа (подписчики, подписки, публикации)
- Tesseract OCR поддерживает русский и английский языки

### Проблемы с подключением к базе данных

- Проверьте, что PostgreSQL контейнер запущен: `docker-compose ps`
- Проверьте логи: `docker-compose logs postgres`

### Проблемы с Telegram ботом

- Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен в `.env`
- Проверьте логи бота: `docker-compose logs client-server`

## Лицензия

MIT

