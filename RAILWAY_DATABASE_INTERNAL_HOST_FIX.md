# Исправление: could not translate host name "postgres.railway.internal"

## ❌ Проблема:

```
could not translate host name "postgres.railway.internal" to address: Name or service not known
```

Parsing Server не может подключиться к базе данных через внутренний хост `postgres.railway.internal`.

---

## 🔍 Причины:

1. **Сервисы находятся в разных проектах Railway**
   - Внутренний хост работает только внутри одного проекта
   - Если база данных и Parsing Server в разных проектах, нужно использовать публичный URL

2. **Нужно использовать DATABASE_PUBLIC_URL вместо DATABASE_URL**
   - Railway предоставляет `DATABASE_PUBLIC_URL` для внешних подключений
   - `DATABASE_URL` использует внутренний хост `postgres.railway.internal`

---

## ✅ Решение:

### Способ 1: Использовать DATABASE_PUBLIC_URL (Рекомендуется)

1. **Откройте сервис базы данных (Postgres)** → **Variables**
2. Найдите переменную `DATABASE_PUBLIC_URL`
3. **Скопируйте** её значение
4. В **Parsing Server** → **Variables** установите:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте значение `DATABASE_PUBLIC_URL`
5. **Сохраните** и **Redeploy**

### Способ 2: Использовать ссылку на DATABASE_PUBLIC_URL

1. **Parsing Server** → **Settings** → **Variables**
2. Добавьте переменную:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_PUBLIC_URL}}`
3. **Сохраните** и **Redeploy**

### Способ 3: Проверить, что сервисы в одном проекте

1. В Railway откройте **Architecture**
2. Убедитесь, что **Postgres** и **VeraliA** находятся в **одном проекте**
3. Если в разных проектах:
   - Используйте `DATABASE_PUBLIC_URL` (см. Способ 1)
   - Или переместите сервисы в один проект

---

## 📋 Пошаговая инструкция:

### Вариант A: Использовать DATABASE_PUBLIC_URL

1. **Postgres** → **Variables** → найдите `DATABASE_PUBLIC_URL`
2. **Скопируйте** значение
3. **Parsing Server** → **Variables**
4. Найдите или создайте `DATABASE_URL`
5. **Вставьте** скопированное значение `DATABASE_PUBLIC_URL`
6. **Сохраните** (галочка ✓)
7. **Redeploy**

### Вариант B: Использовать ссылку

1. **Parsing Server** → **Settings** → **Variables**
2. **+ New Variable** (или редактируйте существующую)
3. **Name**: `DATABASE_URL`
4. **Value**: `${{Postgres.DATABASE_PUBLIC_URL}}`
5. **Сохраните** (галочка ✓)
6. **Redeploy**

---

## 🔍 Разница между DATABASE_URL и DATABASE_PUBLIC_URL:

- **DATABASE_URL**: 
  - Использует внутренний хост `postgres.railway.internal`
  - Работает только внутри одного проекта Railway
  - Быстрее, но не работает между проектами

- **DATABASE_PUBLIC_URL**:
  - Использует публичный хост
  - Работает из любого места (внутри и вне Railway)
  - Медленнее, но универсальнее

---

## ✅ После исправления:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. В логах должно появиться:
   ```
   INFO: Database initialized
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```

---

## 🔍 Проверка:

После деплоя проверьте:
- ✅ В логах НЕ должно быть ошибок про `could not translate host name`
- ✅ Должно быть сообщение `Database initialized`
- ✅ Сервис должен запуститься успешно

---

## ⚠️ Важно:

- Если сервисы в **одном проекте** → можно использовать `DATABASE_URL` (внутренний хост)
- Если сервисы в **разных проектах** → используйте `DATABASE_PUBLIC_URL` (публичный хост)
- **Рекомендуется** использовать `DATABASE_PUBLIC_URL` для надежности

---

## 🎯 Итого:

1. ✅ Используйте `DATABASE_PUBLIC_URL` вместо `DATABASE_URL`
2. ✅ Скопируйте значение из Postgres → Variables
3. ✅ Или используйте ссылку: `${{Postgres.DATABASE_PUBLIC_URL}}`
4. ✅ Сохраните и Redeploy

После этого подключение должно работать!

