# Исправление: DATABASE_URL не установлен для Parsing Server

## ❌ Проблема:

В Variables для Parsing Server есть `DATABASE_PUBLIC_URL`, но нет `DATABASE_URL`.

Код использует переменную `DATABASE_URL`, поэтому нужно добавить её.

---

## ✅ Решение:

### Способ 1: Скопировать значение DATABASE_PUBLIC_URL в DATABASE_URL

1. **Откройте Parsing Server (VeraliA)** → **Variables**
2. Найдите переменную `DATABASE_PUBLIC_URL`
3. **Скопируйте** её значение (полный URL)
4. Нажмите **"+ New Variable"**
5. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение из `DATABASE_PUBLIC_URL`
6. **Сохраните** (галочка ✓)
7. **Redeploy** Parsing Server

### Способ 2: Использовать ссылку

1. **Parsing Server** → **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_PUBLIC_URL}}`
   
   (Используйте точное имя сервиса базы данных)
4. **Сохраните** и **Redeploy**

---

## 📋 Пошаговая инструкция:

1. **Parsing Server (VeraliA)** → **Settings** → **Variables**
2. Найдите переменную `DATABASE_PUBLIC_URL`
3. **Скопируйте** её значение (это будет что-то вроде `postgresql://postgres:password@yamanote.proxy.rlwy.net:32013/railway`)
4. Нажмите **"+ New Variable"**
5. **Name**: `DATABASE_URL`
6. **Value**: вставьте скопированное значение
7. **Сохраните** (галочка ✓)
8. **Redeploy** Parsing Server

---

## ✅ После исправления:

В логах должно появиться:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```

---

## 🔍 Проверка:

1. **Parsing Server** → **Variables**
2. Должны быть **две переменные**:
   - `DATABASE_PUBLIC_URL` (уже есть)
   - `DATABASE_URL` (нужно добавить)
3. **Parsing Server** → **Logs**
4. Не должно быть ошибок про `DATABASE_URL не установлен`
5. Должно быть сообщение `Database initialized`

---

## ⚠️ Важно:

- Код использует переменную `DATABASE_URL`
- Поэтому нужно добавить `DATABASE_URL` со значением из `DATABASE_PUBLIC_URL`
- Можно оставить обе переменные или использовать только `DATABASE_URL`

---

## 🎯 Итого:

1. ✅ Скопируйте значение `DATABASE_PUBLIC_URL`
2. ✅ Создайте новую переменную `DATABASE_URL`
3. ✅ Вставьте скопированное значение
4. ✅ Сохраните и Redeploy

После этого Parsing Server должен подключиться к базе данных!


