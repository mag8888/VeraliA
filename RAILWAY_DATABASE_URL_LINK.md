# Использование ссылки (переменной) для DATABASE_URL

## ✅ Да, можно использовать ссылку!

Railway поддерживает ссылки на переменные других сервисов через синтаксис:
```
${{Имя_Сервиса.Переменная}}
```

---

## 🔧 Как установить ссылку:

### Способ 1: Через Variables (Рекомендуется)

1. **Откройте Parsing Server (VeraliA)** → **Settings** → **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте точное имя сервиса базы данных!

4. **Сохраните** (галочка ✓)

### Способ 2: Через Architecture (Автоматически)

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных **Postgres**
3. **Перетащите стрелку** от **Postgres** к **VeraliA** (Parsing Server)
4. Railway автоматически добавит `DATABASE_URL` в Variables

---

## 🔍 Проверка имени сервиса базы данных:

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных
3. **Запомните точное имя** (с учетом регистра):
   - `Postgres`
   - `PostgreSQL`
   - `postgres`
   - И т.д.

4. Используйте это имя в ссылке:
   ```
   ${{<Точное_Имя>.DATABASE_URL}}
   ```

---

## ⚠️ Если ссылка не работает:

### Проблема 1: Неправильное имя сервиса

**Решение:** Проверьте точное имя в Architecture и используйте его.

### Проблема 2: База данных в другом проекте

**Решение:** Используйте прямое значение (см. ниже).

### Проблема 3: Синтаксис неправильный

**Правильно:**
- `${{Postgres.DATABASE_URL}}` ✅
- `${{PostgreSQL.DATABASE_URL}}` ✅

**Неправильно:**
- `${{ Postgres.DATABASE_URL }}` ❌ (пробелы внутри)
- `{{Postgres.DATABASE_URL}}` ❌ (нет $ в начале)
- `${{Postgres.DATABASE_URL }}` ❌ (пробел в конце)

---

## 🔄 Альтернатива: Прямое значение

Если ссылка не работает, можно использовать прямое значение:

1. Откройте **сервис базы данных (Postgres)** → **Variables**
2. Найдите переменную `DATABASE_URL`
3. **Скопируйте** её значение (полный URL):
   ```
   postgresql://postgres:password@postgres.railway.internal:5432/railway
   ```
4. В **Parsing Server** → **Variables** установите:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение
5. **Сохраните**

---

## ✅ Правильные настройки:

### Для Parsing Server - Variables:

**Вариант A: Ссылка (рекомендуется)**
```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

**Вариант B: Прямое значение (если ссылка не работает)**
```
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
```

---

## 📋 Пошаговая инструкция:

### Использование ссылки:

1. **Parsing Server (VeraliA)** → **Settings** → **Variables**
2. **+ New Variable**
3. **Name**: `DATABASE_URL`
4. **Value**: `${{Postgres.DATABASE_URL}}` (или правильное имя)
5. **Сохраните** (галочка ✓)
6. **Redeploy**

### Или через Architecture:

1. **Architecture**
2. Наведите на **Postgres**
3. Появится стрелка → перетащите к **VeraliA**
4. Railway автоматически добавит переменную
5. **Redeploy**

---

## ✅ После установки:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. В логах должно появиться:
   ```
   INFO: Database initialized
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```

---

## 🎯 Итого:

- ✅ **Можно использовать ссылку**: `${{Postgres.DATABASE_URL}}`
- ✅ **Или прямое значение**: скопировать из Postgres → Variables
- ✅ **Или через Architecture**: перетащить стрелку от Postgres к VeraliA

Попробуйте сначала ссылку, если не работает - используйте прямое значение!

