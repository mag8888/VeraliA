# Исправление Dockerfile Path для Parsing Server

## ⚠️ Важно: Dockerfile Path

В настройках Railway указано:
```
Dockerfile Path: /Dockerfile.parsing
```

## ❌ Проблема:

Ведущий слэш `/` в начале пути может вызвать проблемы.

## ✅ Правильное значение:

```
Dockerfile Path: Dockerfile.parsing
```

**БЕЗ ведущего слэша `/`!**

---

## 📋 Как исправить:

1. Откройте **Parsing Server** в Railway
2. Перейдите в **Settings → Build**
3. В поле **"Dockerfile Path"**:
   - Удалите ведущий слэш `/`
   - Оставьте только: `Dockerfile.parsing`
4. **Сохраните**
5. **Redeploy**

---

## ✅ Правильные настройки:

```
Root Directory: (пусто)
Dockerfile Path: Dockerfile.parsing
Builder: Dockerfile
```

---

## 🔍 После исправления:

Railway должен:
- ✅ Найти `Dockerfile.parsing` в корне проекта
- ✅ Установить `python-multipart` из requirements.txt
- ✅ Запустить Parsing Server без ошибок


