# Какое значение DATABASE_URL использовать для Parsing Server?

## 🔍 Что вы видите в скриншоте:

В Variables для сервиса **Postgres** есть:
```
DATABASE_URL = postgresql://postgres:password@postgres.railway.internal:5432/railway
```

Это **полный URL** базы данных.

---

## ✅ Что нужно сделать:

### Способ 1: Использовать переменную Railway (Рекомендуется)

**НЕ копируйте** значение напрямую!

Вместо этого используйте **переменную Railway**:

1. Откройте **Parsing Server (VeraliA)** → **Settings** → **Variables**
2. Добавьте переменную:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте имя сервиса базы данных (`Postgres`)

### Способ 2: Скопировать значение напрямую (если переменная не работает)

Если `${{Postgres.DATABASE_URL}}` не работает:

1. **Скопируйте** значение `DATABASE_URL` из сервиса Postgres:
   ```
   postgresql://postgres:rGhgiyukeUoLjKMssQcSRYhTfGDHuILF@postgres.railway.internal:5432/railway
   ```

2. В **Parsing Server** → **Variables** установите:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение

---

## ⚠️ Важно:

### Почему лучше использовать переменную:

- ✅ **Автоматическое обновление** - если база данных изменится, URL обновится автоматически
- ✅ **Безопасность** - Railway правильно управляет паролями
- ✅ **Удобство** - не нужно копировать длинные строки

### Когда копировать значение:

- ❌ Если `${{Postgres.DATABASE_URL}}` не разрешается
- ❌ Если база данных в другом проекте
- ❌ Если нужно использовать конкретный URL

---

## 📋 Шаги:

### Вариант A: Переменная Railway (лучше)

1. **Parsing Server** → **Settings** → **Variables**
2. **+ New Variable**
3. **Name**: `DATABASE_URL`
4. **Value**: `${{Postgres.DATABASE_URL}}`
5. **Save** (галочка ✓)
6. **Redeploy**

### Вариант B: Прямое значение (если не работает)

1. **Postgres** → **Variables** → скопируйте `DATABASE_URL`
2. **Parsing Server** → **Variables**
3. **+ New Variable**
4. **Name**: `DATABASE_URL`
5. **Value**: вставьте скопированное значение
6. **Save** (галочка ✓)
7. **Redeploy**

---

## ✅ Проверка:

После установки переменной:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. Проверьте логи:
   - Не должно быть ошибок про `DATABASE_URL не установлен`
   - Должно быть: `Database initialized`

---

## 🎯 Итого:

**Рекомендуется:**
- Использовать `${{Postgres.DATABASE_URL}}` в Parsing Server

**Альтернатива:**
- Скопировать значение `DATABASE_URL` из Postgres и вставить в Parsing Server

**НЕ нужно:**
- Копировать значение, если можно использовать переменную Railway


