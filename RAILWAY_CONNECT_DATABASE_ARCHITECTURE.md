# Подключение базы данных к Parsing Server через Architecture

## ✅ Да, нужно подключить базу данных к Parsing Server!

Самый простой способ - использовать Architecture в Railway для автоматического подключения.

---

## 🔧 Способ 1: Через Architecture (Автоматически) - Рекомендуется

### Пошаговая инструкция:

1. **В Railway откройте ваш проект**
2. Перейдите в **Architecture** (вкладка вверху)
3. Найдите сервис **Postgres** (база данных)
4. Найдите сервис **VeraliA** (Parsing Server)
5. **Наведите курсор** на сервис **Postgres**
6. Появится **стрелка** или **точка подключения**
7. **Перетащите стрелку** от **Postgres** к **VeraliA** (Parsing Server)
8. Railway автоматически:
   - Добавит переменную `DATABASE_URL` в Variables для Parsing Server
   - Установит правильное значение
   - Подключит сервисы

### После подключения:

1. **Parsing Server** → **Variables**
2. Должна появиться переменная `DATABASE_URL`
3. Значение будет автоматически установлено Railway
4. **Redeploy** Parsing Server (или Railway сделает это автоматически)

---

## 🔧 Способ 2: Вручную через Variables

Если Architecture не работает:

1. **Postgres** → **Variables** → найдите `DATABASE_PUBLIC_URL`
2. **Скопируйте** значение
3. **Parsing Server** → **Variables**
4. **+ New Variable**
5. **Name**: `DATABASE_URL`
6. **Value**: вставьте скопированное значение
7. **Сохраните** и **Redeploy**

---

## ✅ Преимущества подключения через Architecture:

- ✅ **Автоматически** добавляет переменную `DATABASE_URL`
- ✅ **Автоматически** обновляет значение при изменении базы данных
- ✅ **Визуально** показывает связь между сервисами
- ✅ **Проще** управлять зависимостями

---

## 🔍 Проверка подключения:

### После подключения через Architecture:

1. **Architecture** → должна быть **стрелка** от Postgres к VeraliA
2. **Parsing Server** → **Variables** → должна быть переменная `DATABASE_URL`
3. **Parsing Server** → **Logs** → должно быть `Database initialized`

---

## 📋 Пошаговая инструкция (подробно):

1. **Откройте Railway** → ваш проект
2. **Architecture** (вкладка вверху)
3. Найдите **Postgres** (сервис базы данных)
4. Найдите **VeraliA** (Parsing Server)
5. **Наведите** на **Postgres** → появится точка/стрелка
6. **Кликните и перетащите** от **Postgres** к **VeraliA**
7. Появится **стрелка подключения**
8. Railway автоматически добавит `DATABASE_URL` в Variables
9. **Redeploy** Parsing Server (или подождите автоматического перезапуска)

---

## ✅ После подключения:

В Variables для Parsing Server должна появиться:
```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

Или прямое значение (если Railway установил его автоматически).

---

## 🎯 Итого:

1. ✅ **Architecture** → перетащите стрелку от **Postgres** к **VeraliA**
2. ✅ Railway автоматически добавит `DATABASE_URL`
3. ✅ **Redeploy** Parsing Server
4. ✅ Проверьте логи - должно быть `Database initialized`

Это самый простой и надежный способ подключения базы данных!

