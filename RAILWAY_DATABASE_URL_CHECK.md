# Проверка настройки DATABASE_URL

## ✅ Первый скриншот: Правильно!

```
DATABASE_URL = ${{PostgreSQL.DATABASE_URL}}
```

**Это правильно!** ✅
- Используется правильный синтаксис `${{...}}`
- Имя сервиса `PostgreSQL` (с заглавными буквами)
- Сборка идет (Building 00:49)

---

## ❌ Второй скриншот: Проблема!

```
DATABASE_URL = <empty string>
```

**Это неправильно!** ❌
- Переменная пустая
- Railway не может разрешить `${{PostgreSQL.DATABASE_URL}}`

---

## 🔍 Возможные причины:

### 1. Неправильное имя сервиса базы данных

Если база данных называется не `PostgreSQL`, переменная не разрешится.

**Решение:** Проверьте точное имя:
1. Откройте **Architecture** в Railway
2. Найдите сервис базы данных
3. Запомните **точное имя** (с учетом регистра)

### 2. База данных не подключена к проекту

Если база данных находится в другом проекте, переменная не будет работать.

**Решение:** Убедитесь, что база данных в том же проекте.

### 3. Переменная не сохранилась

Возможно, изменения не были сохранены.

**Решение:** 
1. Удалите переменную `DATABASE_URL`
2. Добавьте заново:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{PostgreSQL.DATABASE_URL}}`
3. **Сохраните** (нажмите галочку ✓)

---

## ✅ Правильная настройка:

### Шаг 1: Проверьте имя сервиса базы данных

1. **Architecture** → найдите сервис базы данных
2. **Запомните точное имя:**
   - `PostgreSQL` (с заглавными)
   - `Postgres` (только первая заглавная)
   - `postgres` (все строчные)
   - И т.д.

### Шаг 2: Установите переменную

1. **Parsing Server** → **Settings** → **Variables**
2. Найдите `DATABASE_URL`
3. Если пустая → **удалите** и **добавьте заново**
4. **Value**: `${{<Точное_Имя>.DATABASE_URL}}`
5. **Сохраните** (галочка ✓)

### Шаг 3: Проверьте значение

После сохранения значение должно быть:
- `${{PostgreSQL.DATABASE_URL}}` (не `<empty string>`)
- Или реальный URL базы данных

---

## 🔍 Альтернативный способ:

Если `${{PostgreSQL.DATABASE_URL}}` не работает:

1. Откройте **сервис базы данных** (PostgreSQL) → **Variables**
2. Найдите `DATABASE_URL`
3. **Скопируйте** его значение
4. В **Parsing Server** → **Variables** установите:
   - **Name**: `DATABASE_URL`
   - **Value**: вставьте скопированное значение

---

## ✅ После исправления:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. Проверьте логи:
   - Не должно быть ошибок про `DATABASE_URL не установлен`
   - Должно быть: `Database initialized`

---

## 🎯 Итого:

- ✅ Первый скриншот: **Правильно** - используйте `${{PostgreSQL.DATABASE_URL}}`
- ❌ Второй скриншот: **Неправильно** - переменная пустая, нужно исправить
- ✅ Проверьте точное имя сервиса базы данных
- ✅ Убедитесь, что переменная сохранилась (не `<empty string>`)

