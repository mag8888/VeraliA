# Настройка Railway - Пошаговая инструкция с нуля

## 📋 Подготовка

### Что нужно:
1. Аккаунт на Railway
2. Репозиторий на GitHub (уже есть: `mag8888/VeraliA`)
3. Токен Telegram бота от @BotFather

---

## 🗄️ ШАГ 1: Создание базы данных PostgreSQL

### 1.1. Создайте новый проект в Railway

1. Откройте [Railway](https://railway.app)
2. Нажмите **"+ New Project"**
3. Выберите **"Empty Project"** или **"Deploy from GitHub repo"**
4. Если выбрали GitHub → подключите репозиторий `mag8888/VeraliA`

### 1.2. Добавьте базу данных PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Дождитесь создания базы данных (появится сервис "Postgres")

### 1.3. Запомните имя сервиса базы данных

1. Откройте **Architecture**
2. Найдите сервис базы данных
3. **Запомните точное имя** (обычно "Postgres" или "PostgreSQL")

---

## 🤖 ШАГ 2: Настройка Client Server

### 2.1. Создайте сервис Client Server

1. В том же проекте нажмите **"+ New"**
2. Выберите **"GitHub Repo"**
3. Выберите репозиторий `mag8888/VeraliA`
4. Railway создаст новый сервис (назовите его "Client Server" или "client-server")

### 2.2. Настройте Build для Client Server

1. Откройте **Client Server** → **Settings** → **Build**
2. Проверьте настройки:
   - **Root Directory**: оставьте **ПУСТЫМ** (корень проекта)
   - **Builder**: выберите **"Dockerfile"**
   - **Dockerfile Path**: установите `Dockerfile`
3. **Сохраните** изменения

### 2.3. Настройте Variables для Client Server

1. Откройте **Client Server** → **Settings** → **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=${{Postgres.DATABASE_URL}}
PARSING_SERVER_URL=https://parsing-server-production.up.railway.app
MINIAPP_URL=https://client-server-production.up.railway.app/miniapp
```

**Важно:**
- Замените `ваш_токен_от_BotFather` на реальный токен
- Используйте точное имя сервиса базы данных (например, `${{Postgres.DATABASE_URL}}`)
- URL сервисов будут доступны после деплоя

4. **Сохраните** изменения

### 2.4. Деплой Client Server

1. Railway автоматически запустит деплой
2. Дождитесь завершения сборки
3. После деплоя скопируйте **публичный URL** сервиса (например, `https://client-server-production.up.railway.app`)

---

## 🔧 ШАГ 3: Настройка Parsing Server

### 3.1. Создайте сервис Parsing Server

1. В том же проекте нажмите **"+ New"**
2. Выберите **"GitHub Repo"**
3. Выберите тот же репозиторий `mag8888/VeraliA`
4. Railway создаст новый сервис (назовите его "Parsing Server" или "parsing-server")

### 3.2. Настройте Build для Parsing Server

1. Откройте **Parsing Server** → **Settings** → **Build**
2. Проверьте настройки:
   - **Root Directory**: оставьте **ПУСТЫМ** (корень проекта)
   - **Builder**: выберите **"Dockerfile"**
   - **Dockerfile Path**: установите `Dockerfile.parsing` ⚠️ **ВАЖНО!**
3. **Сохраните** изменения
4. **Обновите страницу** (F5) и проверьте, что значение сохранилось

### 3.3. Настройте Variables для Parsing Server

1. Откройте **Parsing Server** → **Settings** → **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте переменную:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

**Важно:**
- Используйте точное имя сервиса базы данных
- Если `DATABASE_URL` не работает, используйте `DATABASE_PUBLIC_URL`

4. **Сохраните** изменения

### 3.4. Деплой Parsing Server

1. Railway автоматически запустит деплой
2. Дождитесь завершения сборки
3. После деплоя скопируйте **публичный URL** сервиса (например, `https://parsing-server-production.up.railway.app`)

---

## 🔗 ШАГ 4: Обновление URL в Client Server

### 4.1. Обновите PARSING_SERVER_URL

1. Откройте **Client Server** → **Settings** → **Variables**
2. Найдите переменную `PARSING_SERVER_URL`
3. Обновите значение на реальный URL Parsing Server:
   ```
   PARSING_SERVER_URL=https://parsing-server-production.up.railway.app
   ```
4. **Сохраните** изменения
5. Railway автоматически перезапустит сервис

### 4.2. Обновите MINIAPP_URL

1. В тех же Variables найдите `MINIAPP_URL`
2. Обновите значение на реальный URL Client Server:
   ```
   MINIAPP_URL=https://client-server-production.up.railway.app/miniapp
   ```
3. **Сохраните** изменения

---

## ✅ ШАГ 5: Проверка работы

### 5.1. Проверьте логи Client Server

1. Откройте **Client Server** → **Logs**
2. Должно быть:
   ```
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```
3. Не должно быть ошибок про `TELEGRAM_BOT_TOKEN`

### 5.2. Проверьте логи Parsing Server

1. Откройте **Parsing Server** → **Logs**
2. Должно быть:
   ```
   INFO: Database initialized
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```
3. Не должно быть ошибок про `TELEGRAM_BOT_TOKEN` или `DATABASE_URL`

### 5.3. Проверьте работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить

---

## 🔍 Чек-лист настройки

### Client Server:
- [ ] Root Directory: **ПУСТО**
- [ ] Dockerfile Path: `Dockerfile`
- [ ] Builder: **Dockerfile**
- [ ] Variables:
  - [ ] `TELEGRAM_BOT_TOKEN` установлен
  - [ ] `DATABASE_URL=${{Postgres.DATABASE_URL}}`
  - [ ] `PARSING_SERVER_URL` установлен
  - [ ] `MINIAPP_URL` установлен

### Parsing Server:
- [ ] Root Directory: **ПУСТО**
- [ ] Dockerfile Path: `Dockerfile.parsing` ⚠️ **КРИТИЧНО!**
- [ ] Builder: **Dockerfile**
- [ ] Variables:
  - [ ] `DATABASE_URL=${{Postgres.DATABASE_URL}}`

### База данных:
- [ ] PostgreSQL создана
- [ ] Имя сервиса запомнено
- [ ] Сервис работает (зеленая галочка)

---

## ⚠️ Частые ошибки и решения

### Ошибка 1: TELEGRAM_BOT_TOKEN не установлен (Parsing Server)

**Причина:** Используется неправильный Dockerfile (`Dockerfile` вместо `Dockerfile.parsing`)

**Решение:**
1. Parsing Server → Settings → Build
2. Dockerfile Path: измените на `Dockerfile.parsing`
3. Сохраните и Redeploy

### Ошибка 2: DATABASE_URL не установлен

**Причина:** Переменная не установлена или имя сервиса неправильное

**Решение:**
1. Проверьте точное имя сервиса базы данных
2. Установите `DATABASE_URL=${{Postgres.DATABASE_URL}}` (или правильное имя)
3. Сохраните и Redeploy

### Ошибка 3: could not translate host name "postgres.railway.internal"

**Причина:** Используется внутренний хост, который не работает

**Решение:**
1. Используйте `DATABASE_PUBLIC_URL` вместо `DATABASE_URL`
2. Или скопируйте значение `DATABASE_PUBLIC_URL` из Postgres → Variables

---

## 📝 Итоговые настройки

### Client Server:
```
Root Directory: (пусто)
Dockerfile Path: Dockerfile
Builder: Dockerfile
Variables:
  TELEGRAM_BOT_TOKEN=ваш_токен
  DATABASE_URL=${{Postgres.DATABASE_URL}}
  PARSING_SERVER_URL=https://parsing-server-production.up.railway.app
  MINIAPP_URL=https://client-server-production.up.railway.app/miniapp
```

### Parsing Server:
```
Root Directory: (пусто)
Dockerfile Path: Dockerfile.parsing
Builder: Dockerfile
Variables:
  DATABASE_URL=${{Postgres.DATABASE_URL}}
```

---

## 🎯 Готово!

После выполнения всех шагов оба сервиса должны работать:
- ✅ Client Server запущен и отвечает на команды бота
- ✅ Parsing Server запущен и подключен к базе данных
- ✅ База данных работает

Если что-то не работает - проверьте логи и настройки по чек-листу выше.

