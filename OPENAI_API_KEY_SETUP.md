# Настройка OpenAI API Key для GPT анализа

## 📋 Где вставить API ключ:

### Railway (Parsing Server)

1. **Railway Dashboard** → Найдите сервис **Parsing Server**
2. Перейдите в **Settings** → **Variables**
3. Нажмите **"+ New Variable"**
4. Добавьте переменную:
   - **Name**: `OPENAI_API_KEY`
   - **Value**: Ваш API ключ от OpenAI (начинается с `sk-...`)
5. **Сохраните** изменения

### Опционально: Выбор модели GPT

Вы можете также добавить переменную для выбора модели:

- **Name**: `OPENAI_MODEL`
- **Value**: 
  - `gpt-4o-mini` (по умолчанию, дешевле)
  - `gpt-4o` (более качественный, дороже)
  - `gpt-4-turbo` (альтернатива)

---

## 🔑 Как получить API ключ OpenAI:

1. Перейдите на https://platform.openai.com/
2. Войдите в свой аккаунт (или создайте новый)
3. Перейдите в **API Keys** (в меню слева)
4. Нажмите **"+ Create new secret key"**
5. Скопируйте ключ (он показывается только один раз!)
6. Вставьте его в Railway как переменную `OPENAI_API_KEY`

---

## ✅ Проверка работы:

После добавления ключа:

1. **Redeploy** Parsing Server
2. Загрузите скриншот профиля
3. Проверьте логи Parsing Server:
   - Должно быть: `GPT отчет успешно сгенерирован`
   - Если ошибка: `OPENAI_API_KEY не установлен` → проверьте переменную

---

## 💰 Стоимость:

- **gpt-4o-mini**: ~$0.15 за 1M входных токенов, ~$0.60 за 1M выходных
- **gpt-4o**: ~$2.50 за 1M входных токенов, ~$10 за 1M выходных

Один отчет обычно использует:
- Входные токены: ~500-1000
- Выходные токены: ~2000-3000

**Примерная стоимость одного отчета:**
- gpt-4o-mini: ~$0.001-0.002 (очень дешево)
- gpt-4o: ~$0.01-0.03

---

## ⚠️ Важно:

- **Не публикуйте** API ключ в коде или в репозитории
- Храните ключ только в переменных окружения Railway
- Если ключ скомпрометирован, удалите его и создайте новый на platform.openai.com

---

## 🔄 После настройки:

1. Railway автоматически перезапустит Parsing Server
2. При следующей загрузке скриншота будет сгенерирован GPT отчет
3. Отчет сохранится в базу данных на русском и английском языках
4. Отчет будет доступен в мини-приложении и через API

