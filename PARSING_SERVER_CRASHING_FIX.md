# Исправление: Parsing Server постоянно падает

## ❌ Проблема:

Parsing Server постоянно падает (Crashed каждые несколько секунд).

Это **НЕ нормально** - сервер должен работать стабильно.

---

## 🔍 Причины постоянных падений:

### 1. Ошибка подключения к базе данных
- `DATABASE_URL` неправильный или недоступен
- Внутренний хост не работает
- База данных недоступна

### 2. Ошибка при инициализации базы данных
- Проблемы с созданием таблиц
- Ошибки в SQL запросах

### 3. Ошибка в коде приложения
- Исключения при запуске
- Проблемы с зависимостями

### 4. Недостаточно ресурсов
- Нехватка памяти
- Превышение лимитов Railway

---

## ✅ Решение:

### Шаг 1: Проверьте логи Parsing Server

1. **Parsing Server (VeraliA)** → **Logs**
2. Найдите **последнюю ошибку** перед падением
3. Скопируйте текст ошибки

### Шаг 2: Проверьте DATABASE_URL

1. **Parsing Server** → **Variables**
2. Проверьте, что `DATABASE_URL` установлен
3. Убедитесь, что используется **публичный URL** (не `postgres.railway.internal`)
4. Если используется внутренний хост → замените на `DATABASE_PUBLIC_URL`

### Шаг 3: Проверьте базу данных

1. **Postgres** → **Logs**
2. Убедитесь, что база данных работает
3. Не должно быть ошибок

### Шаг 4: Исправьте проблему

В зависимости от ошибки в логах:

#### Если ошибка: "could not translate host name"
**Решение:** Используйте `DATABASE_PUBLIC_URL` вместо `DATABASE_URL` с внутренним хостом

#### Если ошибка: "DATABASE_URL не установлен"
**Решение:** Добавьте переменную `DATABASE_URL` в Variables

#### Если ошибка: "Database initialization failed"
**Решение:** Проверьте права доступа к базе данных

#### Если ошибка: "Out of memory" или "Resource limit"
**Решение:** Увеличьте ресурсы сервиса в Railway

---

## 📋 Пошаговая диагностика:

### 1. Откройте логи

1. **Parsing Server (VeraliA)** → **Logs**
2. Прокрутите до **начала** последнего деплоя
3. Найдите **первую ошибку**

### 2. Определите тип ошибки

- **Ошибка подключения к БД** → исправьте `DATABASE_URL`
- **Ошибка в коде** → проверьте код приложения
- **Ошибка ресурсов** → увеличьте ресурсы

### 3. Исправьте проблему

Следуйте инструкциям выше в зависимости от типа ошибки.

### 4. Redeploy

1. После исправления **Redeploy** сервис
2. Проверьте логи - ошибка должна исчезнуть
3. Сервис должен работать стабильно

---

## 🔍 Частые ошибки и решения:

### Ошибка 1: "could not translate host name postgres.railway.internal"

**Причина:** Используется внутренний хост

**Решение:**
1. **Postgres** → **Variables** → скопируйте `DATABASE_PUBLIC_URL`
2. **Parsing Server** → **Variables** → `DATABASE_URL` → вставьте публичный URL
3. **Сохраните** и **Redeploy**

### Ошибка 2: "DATABASE_URL не установлен"

**Причина:** Переменная не установлена

**Решение:**
1. **Parsing Server** → **Variables**
2. Добавьте `DATABASE_URL` со значением из `DATABASE_PUBLIC_URL`
3. **Сохраните** и **Redeploy**

### Ошибка 3: "Application startup failed"

**Причина:** Ошибка при запуске приложения

**Решение:**
1. Проверьте логи - должна быть конкретная ошибка
2. Исправьте проблему в коде или настройках
3. **Redeploy**

---

## ✅ После исправления:

Сервис должен:
- ✅ Запуститься без ошибок
- ✅ Подключиться к базе данных
- ✅ Работать стабильно (не падать)
- ✅ В логах должно быть: `Database initialized` и `Application startup complete`

---

## 🎯 Итого:

1. ✅ **Проверьте логи** - найдите причину падения
2. ✅ **Исправьте проблему** (чаще всего это `DATABASE_URL`)
3. ✅ **Redeploy** сервис
4. ✅ **Проверьте**, что сервер работает стабильно

**Постоянные падения - это НЕ нормально!** Нужно найти и исправить причину.

---

## 📝 Что проверить:

- [ ] Логи Parsing Server - какая ошибка?
- [ ] DATABASE_URL установлен и правильный?
- [ ] База данных работает?
- [ ] Код приложения без ошибок?
- [ ] Достаточно ресурсов?

После проверки всех пунктов сервер должен работать стабильно!


