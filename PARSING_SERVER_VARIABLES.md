# Variables для Parsing Server

## ✅ Что добавить в Variables для Parsing Server:

### Обязательные переменные:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

---

## 📋 Как добавить:

1. Откройте **Parsing Server** в Railway
2. Перейдите в **Variables**
3. Нажмите **+ New Variable**
4. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
5. Сохраните

---

## ✅ Что Railway добавит автоматически:

Railway автоматически добавит (не нужно указывать вручную):
- `PORT` - порт для приложения
- `RAILWAY_ENVIRONMENT` - окружение (production)
- `RAILWAY_PROJECT_ID` - ID проекта
- И другие системные переменные

---

## 🔍 Итого для Parsing Server:

**Только 1 переменная вручную:**
- ✅ `DATABASE_URL=${{Postgres.DATABASE_URL}}`

**Остальное Railway добавит автоматически!**

---

## ⚠️ Важно:

- `PORT` **НЕ нужно** добавлять — Railway установит автоматически
- Используйте `${{Postgres.DATABASE_URL}}` для подключения к базе данных
- Убедитесь, что база данных Postgres создана и подключена к проекту


