# Подключение базы данных к Parsing Server в Railway

## ✅ Ответ: НЕ нужно создавать отдельную базу!

**Оба сервиса (Client Server и Parsing Server) могут использовать одну и ту же базу данных.**

---

## 🔍 Проблема:

База данных Postgres создана, но **не подключена** к сервису Parsing Server.

---

## ✅ Решение: Подключите существующую базу данных

### Способ 1: Через Variables (Рекомендуется)

1. **Откройте Parsing Server (VeraliA)** в Railway
2. Перейдите в **Settings → Variables**
3. Нажмите **"+ New Variable"**
4. Добавьте:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте точное имя сервиса базы данных!

### Способ 2: Через Architecture (Автоматическое подключение)

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных **Postgres**
3. **Перетащите** стрелку от **Postgres** к **VeraliA** (Parsing Server)
4. Railway автоматически добавит `DATABASE_URL` в Variables

---

## 🔍 Как проверить имя сервиса базы данных:

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных
3. **Запомните точное имя** (с учетом регистра):
   - `Postgres`
   - `PostgreSQL`
   - `postgres`
   - И т.д.

4. Используйте это имя в переменной:
   ```
   ${{<Точное_Имя>.DATABASE_URL}}
   ```

---

## 📋 Шаги для подключения:

### Вариант A: Через Variables (вручную)

1. **Parsing Server** → **Settings** → **Variables**
2. **+ New Variable**
3. **Name**: `DATABASE_URL`
4. **Value**: `${{Postgres.DATABASE_URL}}` (или правильное имя)
5. **Save**
6. **Redeploy**

### Вариант B: Через Architecture (автоматически)

1. **Architecture**
2. Наведите на сервис **Postgres**
3. Появится стрелка → перетащите к **VeraliA**
4. Railway автоматически добавит переменную
5. **Redeploy**

---

## ✅ После подключения:

1. **Сохраните** изменения
2. **Redeploy** Parsing Server
3. В логах должно появиться:
   ```
   INFO: Database initialized
   INFO: Started server process [1]
   INFO: Application startup complete.
   ```

---

## 🔍 Проверка:

После деплоя проверьте:
- В **Variables** для Parsing Server должна быть переменная `DATABASE_URL`
- В логах не должно быть ошибок про `DATABASE_URL не установлен`
- Должно быть сообщение `Database initialized`

---

## ⚠️ Важно:

- **Одна база данных** для обоих сервисов — это нормально
- **Не нужно** создавать отдельную базу для Parsing Server
- **Нужно** подключить существующую базу через Variables или Architecture

---

## 🎯 Итого:

1. ✅ База данных уже создана
2. ❌ База данных НЕ подключена к Parsing Server
3. ✅ Нужно добавить `DATABASE_URL=${{Postgres.DATABASE_URL}}` в Variables
4. ✅ Или подключить через Architecture (перетащить стрелку)

