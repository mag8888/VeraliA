# Исправление ошибки подключения к базе данных

## ❌ Проблема:

Parsing Server не может подключиться к базе данных PostgreSQL.

---

## 🔍 Диагностика:

### Шаг 1: Проверьте, что DATABASE_URL установлен

1. **Parsing Server** → **Settings** → **Variables**
2. Проверьте, что есть переменная `DATABASE_URL`
3. Проверьте, что значение не пустое

### Шаг 2: Проверьте формат URL

Значение `DATABASE_URL` должно быть в формате:
```
postgresql://username:password@host:port/database
```

Или:
```
postgres://username:password@host:port/database
```

### Шаг 3: Проверьте, что база данных работает

1. **Postgres** → **Logs**
2. Убедитесь, что база данных запущена
3. Не должно быть ошибок

---

## ✅ Решение:

### Способ 1: Использовать DATABASE_PUBLIC_URL (Рекомендуется)

1. **Postgres** → **Variables** → найдите `DATABASE_PUBLIC_URL`
2. **Скопируйте** значение
3. **Parsing Server** → **Variables**
4. Найдите или создайте `DATABASE_URL`
5. **Вставьте** скопированное значение `DATABASE_PUBLIC_URL`
6. **Сохраните** и **Redeploy**

### Способ 2: Проверить и исправить формат URL

1. **Parsing Server** → **Variables** → `DATABASE_URL`
2. Проверьте формат:
   - Должно начинаться с `postgresql://` или `postgres://`
   - Не должно быть пробелов
   - Должен быть полный URL с хостом, портом и базой данных
3. Если формат неправильный → исправьте
4. **Сохраните** и **Redeploy**

### Способ 3: Использовать ссылку на переменную

1. **Parsing Server** → **Variables**
2. Найдите или создайте `DATABASE_URL`
3. Установите значение:
   ```
   ${{Postgres.DATABASE_PUBLIC_URL}}
   ```
   (Используйте точное имя сервиса базы данных)
4. **Сохраните** и **Redeploy**

---

## 📋 Пошаговая инструкция (Способ 1):

1. **Postgres** → **Variables**
2. Найдите `DATABASE_PUBLIC_URL`
3. **Скопируйте** значение (полный URL)
4. **Parsing Server** → **Variables**
5. Найдите `DATABASE_URL` (или создайте новую)
6. **Вставьте** скопированное значение
7. **Сохраните** (галочка ✓)
8. **Redeploy** Parsing Server

---

## 🔍 Проверка формата URL:

### Правильный формат:
```
postgresql://postgres:password@yamanote.proxy.rlwy.net:32013/railway
```

### Неправильный формат:
```
postgres://postgres:password@yamanote.proxy.rlwy.net:32013/railway  (может не работать)
postgresql://postgres:password@yamanote.proxy.rlwy.net:32013  (нет имени базы данных)
postgresql://postgres:password@yamanote.proxy.rlwy.net:32013/railway  (пробелы в пароле)
```

---

## ⚠️ Частые проблемы:

### Проблема 1: Пробел в пароле

Если в URL есть пробел в пароле, это может вызвать проблемы.

**Решение:** Используйте `DATABASE_PUBLIC_URL` из Postgres → Variables (Railway правильно экранирует символы)

### Проблема 2: Неправильный протокол

SQLAlchemy требует `postgresql://`, но Railway может предоставлять `postgres://`.

**Решение:** Код автоматически конвертирует `postgres://` → `postgresql://`

### Проблема 3: База данных недоступна

**Решение:** 
- Проверьте, что Postgres запущен
- Проверьте логи Postgres
- Убедитесь, что используете `DATABASE_PUBLIC_URL` (публичный хост)

---

## ✅ После исправления:

В логах должно появиться:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```

---

## 🔍 Проверка:

1. **Parsing Server** → **Logs**
2. Не должно быть ошибок про подключение к базе данных
3. Должно быть сообщение `Database initialized`
4. Сервис должен работать

---

## 🎯 Итого:

1. ✅ Используйте `DATABASE_PUBLIC_URL` из Postgres → Variables
2. ✅ Скопируйте значение в `DATABASE_URL` для Parsing Server
3. ✅ Проверьте формат URL (должен быть полным)
4. ✅ Сохраните и Redeploy

После этого подключение должно работать!


