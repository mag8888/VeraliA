# Настройка хранилища для картинок

## 📁 Структура хранилища:

```
client-server/
├── uploads/          # Загруженные пользователями скриншоты
├── examples/          # Примеры скриншотов для показа пользователям
└── ...
```

---

## 🖼️ Добавление примеров скриншотов:

### Шаг 1: Подготовьте примеры скриншотов

1. Создайте два скриншота:
   - **Скриншот 1**: Профиль Instagram с основной статистикой
   - **Скриншот 2**: Профессиональная панель Instagram

2. Сохраните их в формате JPG или PNG

### Шаг 2: Добавьте файлы в проект

1. Поместите скриншоты в директорию `client-server/examples/`
2. Назовите файлы, например:
   - `example_profile.jpg` - скриншот профиля
   - `example_professional.jpg` - скриншот профессиональной панели

### Шаг 3: Закоммитьте файлы

```bash
git add client-server/examples/
git commit -m "Добавлены примеры скриншотов"
git push
```

---

## 🔧 Настройка хранилища на Railway:

### Вариант 1: Использовать локальное хранилище (временное)

- Файлы хранятся в контейнере
- **Минус**: Файлы удаляются при перезапуске контейнера
- **Плюс**: Просто настроить

### Вариант 2: Использовать Railway Volume (постоянное)

1. **Client Server** → **Settings** → **Volumes**
2. Нажмите **"+ New Volume"**
3. Создайте volume для `uploads/`
4. Примонтируйте к `/app/uploads`

### Вариант 3: Использовать внешнее хранилище (S3, Cloudinary и т.д.)

Для продакшена рекомендуется использовать внешнее хранилище:
- AWS S3
- Cloudinary
- Railway Blob Storage

---

## 📋 Текущая настройка:

### Локальное хранилище (в контейнере):

- `uploads/` - загруженные скриншоты
- `examples/` - примеры скриншотов

### После деплоя:

1. Примеры скриншотов будут доступны из кода
2. Загруженные скриншоты будут сохраняться в `uploads/`
3. При перезапуске контейнера загруженные файлы удалятся (если не используется Volume)

---

## ✅ Что сделано:

1. ✅ Создана директория `examples/` для примеров скриншотов
2. ✅ Обновлен код бота для показа примеров при знакомстве
3. ✅ Настроено хранилище `uploads/` для загруженных файлов
4. ✅ Обновлен Dockerfile для создания директорий

---

## 🎯 Следующие шаги:

1. Добавьте примеры скриншотов в `client-server/examples/`
2. Закоммитьте и запушьте изменения
3. После деплоя бот будет показывать примеры при знакомстве

---

## ⚠️ Важно:

- Примеры скриншотов должны быть в формате JPG или PNG
- Размер файлов не должен быть слишком большим (рекомендуется < 5MB)
- Для постоянного хранения загруженных файлов используйте Railway Volume или внешнее хранилище

