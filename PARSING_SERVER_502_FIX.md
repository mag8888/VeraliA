# Исправление ошибки 502: Parsing Server не отвечает

## ❌ Проблема:

```
Ошибка: {"status":"error","code":502,"message":"Application failed to respond"}
```

Это означает, что Parsing Server не отвечает на запросы от Client Server.

---

## ✅ Решение:

### Шаг 1: Проверьте, что Parsing Server запущен

1. **Railway Dashboard** → Найдите сервис **Parsing Server**
2. Проверьте **Logs**:
   - Должно быть: `INFO:     Uvicorn running on http://0.0.0.0:8001`
   - Должно быть: `INFO:     Application startup complete.`
3. Если сервис не запущен → **Redeploy**

### Шаг 2: Проверьте PARSING_SERVER_URL в Client Server

1. **Client Server** → **Settings** → **Variables**
2. Найдите переменную `PARSING_SERVER_URL`
3. Убедитесь, что значение правильное:
   - Если Parsing Server в том же проекте Railway: `${{ParsingServer.RAILWAY_PUBLIC_DOMAIN}}`
   - Или прямой URL: `https://parsing-server-production.up.railway.app`
4. **Важно**: URL должен быть **публичным** (не внутренним)

### Шаг 3: Проверьте публичный домен Parsing Server

1. **Parsing Server** → **Settings** → **Networking**
2. Убедитесь, что есть **публичный домен**
3. Если нет → нажмите **"Generate Domain"**
4. Скопируйте публичный URL

### Шаг 4: Обновите PARSING_SERVER_URL

1. **Client Server** → **Variables**
2. Обновите `PARSING_SERVER_URL` на публичный URL Parsing Server
3. **Сохраните** и **Redeploy** Client Server

---

## 🔍 Диагностика:

### Проверка доступности Parsing Server:

1. Откройте в браузере: `https://ваш-parsing-server-url.up.railway.app/health`
2. Должен вернуться: `{"status":"healthy"}`

### Проверка логов Parsing Server:

1. **Parsing Server** → **Logs**
2. Ищите ошибки:
   - `Database connection error`
   - `Module not found`
   - `Import error`

---

## ⚠️ Частые причины ошибки 502:

1. **Parsing Server не запущен** → Redeploy
2. **Неправильный PARSING_SERVER_URL** → Обновите переменную
3. **Parsing Server упал при обработке** → Проверьте логи
4. **Таймаут слишком короткий** → Уже исправлено (120 секунд)
5. **База данных не подключена** → Проверьте DATABASE_URL в Parsing Server

---

## 📋 Чек-лист:

- [ ] Parsing Server запущен (проверьте логи)
- [ ] Parsing Server имеет публичный домен
- [ ] PARSING_SERVER_URL правильный в Client Server
- [ ] DATABASE_URL настроен в Parsing Server
- [ ] Оба сервиса перезапущены после изменений

---

## ✅ После исправления:

1. **Redeploy** оба сервиса
2. Проверьте логи Client Server
3. Попробуйте загрузить скриншот снова
4. Ошибка 502 должна исчезнуть

---

## 🎯 Итого:

1. ✅ Проверьте, что Parsing Server запущен
2. ✅ Проверьте публичный URL Parsing Server
3. ✅ Обновите PARSING_SERVER_URL в Client Server
4. ✅ Redeploy оба сервиса

После этого ошибка 502 должна быть исправлена!

