# Исправление подключения к базе данных для Parsing Server

## ❌ Проблема:

```
could not translate host name "postgres.railway.internal" to address: Name or service not known
```

Parsing Server не может подключиться к базе данных PostgreSQL.

---

## 🔍 Причины:

1. **База данных Postgres не создана** в Railway
2. **База данных не подключена** к проекту Parsing Server
3. **Переменная `DATABASE_URL` не установлена** или установлена неправильно

---

## ✅ Решение:

### Шаг 1: Создайте базу данных Postgres в Railway

1. В Railway откройте ваш проект
2. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Дождитесь создания базы данных

### Шаг 2: Подключите базу данных к Parsing Server

1. Откройте **Parsing Server** в Railway
2. Перейдите в **Settings → Variables**
3. Добавьте переменную:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте `${{Postgres.DATABASE_URL}}` (с двойными фигурными скобками)

### Шаг 3: Проверьте имя сервиса базы данных

Если база данных называется не `Postgres`, используйте правильное имя:
- `${{PostgreSQL.DATABASE_URL}}` (если база называется PostgreSQL)
- `${{<Имя_Базы>.DATABASE_URL}}` (замените на реальное имя)

---

## 🔍 Как проверить имя базы данных:

1. В Railway откройте ваш проект
2. Найдите сервис базы данных (обычно называется "Postgres" или "PostgreSQL")
3. Откройте его → **Variables**
4. Найдите переменную `DATABASE_URL` — там будет полный URL подключения

---

## 📋 Правильная настройка Variables для Parsing Server:

```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

Или если база называется иначе:
```
DATABASE_URL=${{PostgreSQL.DATABASE_URL}}
```

---

## ⚠️ Важно:

- **НЕ используйте** `postgres.railway.internal` напрямую
- **Используйте** `${{Postgres.DATABASE_URL}}` для автоматического подключения
- Railway автоматически подставит правильный URL базы данных

---

## ✅ После исправления:

1. **Сохраните** переменные
2. **Redeploy** Parsing Server
3. В логах должно появиться: `Database initialized`
4. Ошибка подключения должна исчезнуть

---

## 🔍 Проверка в логах:

После успешного подключения в логах должно быть:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```


