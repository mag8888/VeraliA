# Настройка PORT в Railway

## 🎯 Как заполнить PORT в Railway

### Вариант 1: НЕ УКАЗЫВАТЬ PORT (Рекомендуется)

**Railway автоматически устанавливает переменную `PORT`** для каждого сервиса.

#### Что делать:
- **НЕ добавляйте** переменную `PORT` в Variables
- Railway сам установит `PORT` при деплое
- Приложение должно читать `PORT` из окружения: `os.getenv('PORT', '8000')`

#### Преимущества:
- ✅ Автоматическая настройка
- ✅ Railway сам назначает порт
- ✅ Не нужно вручную указывать

---

### Вариант 2: Указать PORT вручную

Если нужно указать конкретный порт:

#### Для Client Server:
```
PORT=8000
```

#### Для Parsing Server:
```
PORT=8001
```

#### Когда использовать:
- Если приложение требует конкретный порт
- Если есть проблемы с автоматическим определением порта

---

## 📝 Как это работает в коде

### Правильный код (читает PORT из окружения):

```python
import os

# Читаем PORT из переменных окружения, по умолчанию 8000
PORT = int(os.getenv('PORT', '8000'))

# Запускаем на этом порту
uvicorn.run(app, host='0.0.0.0', port=PORT)
```

---

## ✅ Рекомендация для вашего проекта

### Client Server:
- **НЕ добавляйте** `PORT` в Variables
- Railway установит автоматически
- Код должен использовать: `os.getenv('PORT', '8000')`

### Parsing Server:
- **НЕ добавляйте** `PORT` в Variables
- Railway установит автоматически
- Код должен использовать: `os.getenv('PORT', '8001')`

---

## 🔍 Проверка

После деплоя проверьте логи Railway:
- Должно быть: `Uvicorn running on http://0.0.0.0:XXXXX`
- Где `XXXXX` — порт, который Railway назначил автоматически

---

## ⚠️ Важно

- Railway **всегда** предоставляет переменную `PORT`
- Не нужно указывать порт в Dockerfile `EXPOSE` (это только для документации)
- Приложение должно слушать на `0.0.0.0`, а не `127.0.0.1`

