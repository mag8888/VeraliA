# Исправление подключения к базе данных Railway

## ✅ База данных создана и добавлена

## ❌ Возможные проблемы:

### 1. DATABASE_URL установлен напрямую вместо переменной Railway

**Неправильно:**
```
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
```

**Правильно:**
```
DATABASE_URL=${{Postgres.DATABASE_URL}}
```

### 2. Использование `postgres.railway.internal` вместо публичного URL

Railway может использовать внутренний хост `postgres.railway.internal`, который работает только внутри одного проекта Railway.

**Решение:** Используйте `${{Postgres.DATABASE_URL}}` - Railway автоматически подставит правильный URL.

### 3. Пробел в пароле базы данных

Если в DATABASE_URL есть пробел в пароле, это может вызвать проблемы.

**Решение:** Используйте `${{Postgres.DATABASE_URL}}` - Railway правильно экранирует все символы.

---

## ✅ Правильная настройка:

### Для Parsing Server:

1. Откройте **Parsing Server (VeraliA)** в Railway
2. Перейдите в **Settings → Variables**
3. Найдите переменную `DATABASE_URL`
4. **Удалите** текущее значение (если оно установлено напрямую)
5. Установите:
   - **Name**: `DATABASE_URL`
   - **Value**: `${{Postgres.DATABASE_URL}}`
   
   **Важно:** Используйте двойные фигурные скобки `{{ }}`

### Если база данных называется иначе:

Если база данных называется не `Postgres`, используйте правильное имя:
- `${{PostgreSQL.DATABASE_URL}}`
- `${{<Имя_Сервиса>.DATABASE_URL}}`

---

## 🔍 Как проверить имя сервиса базы данных:

1. В Railway откройте **Architecture**
2. Найдите сервис базы данных (обычно "Postgres" или "PostgreSQL")
3. Откройте его → **Variables**
4. Там будет переменная `DATABASE_URL` - используйте имя этого сервиса

---

## ⚠️ Важно:

- **НЕ копируйте** значение DATABASE_URL напрямую
- **Используйте** `${{Postgres.DATABASE_URL}}` для автоматического подключения
- Railway автоматически обновит URL при изменении базы данных

---

## 📋 Шаги:

1. **Parsing Server → Settings → Variables**
2. **Удалите** текущий `DATABASE_URL` (если установлен напрямую)
3. **Добавьте** новый:
   - Name: `DATABASE_URL`
   - Value: `${{Postgres.DATABASE_URL}}`
4. **Сохраните**
5. **Redeploy** Parsing Server

---

## ✅ После исправления:

В логах должно появиться:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```

Ошибка `could not translate host name "postgres.railway.internal"` должна исчезнуть.

---

## 🔍 Проверка:

После деплоя проверьте логи:
- Не должно быть ошибок подключения к базе данных
- Должно быть сообщение "Database initialized"

