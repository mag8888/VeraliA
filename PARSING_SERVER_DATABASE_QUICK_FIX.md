# Быстрое исправление подключения к базе данных для Parsing Server

## ❌ Проблема:

Parsing Server не может подключиться к базе данных PostgreSQL.

---

## ✅ Решение (3 способа):

### Способ 1: Использовать DATABASE_PUBLIC_URL (Рекомендуется)

1. **Откройте сервис базы данных (Postgres)** → **Variables**
2. Найдите переменную `DATABASE_PUBLIC_URL`
3. **Скопируйте** её значение (полный URL)
4. В **Parsing Server** → **Variables**:
   - Найдите или создайте `DATABASE_URL`
   - **Вставьте** скопированное значение `DATABASE_PUBLIC_URL`
5. **Сохраните** и **Redeploy**

### Способ 2: Использовать ссылку на DATABASE_PUBLIC_URL

1. **Parsing Server** → **Settings** → **Variables**
2. Найдите или создайте `DATABASE_URL`
3. Установите значение:
   ```
   ${{Postgres.DATABASE_PUBLIC_URL}}
   ```
   (Используйте точное имя сервиса базы данных)
4. **Сохраните** и **Redeploy**

### Способ 3: Проверить подключение через Architecture

1. В Railway откройте **Architecture**
2. Убедитесь, что **Postgres** и **Parsing Server** находятся в **одном проекте**
3. Если в одном проекте:
   - Попробуйте перетащить стрелку от **Postgres** к **Parsing Server**
   - Railway автоматически добавит переменную
4. **Redeploy**

---

## 📋 Пошаговая инструкция (Способ 1 - самый надежный):

1. **Postgres** → **Variables** → найдите `DATABASE_PUBLIC_URL`
2. **Скопируйте** значение (это будет что-то вроде `postgresql://postgres:password@...`)
3. **Parsing Server** → **Variables**
4. Найдите `DATABASE_URL` (или создайте новую)
5. **Вставьте** скопированное значение `DATABASE_PUBLIC_URL`
6. **Сохраните** (галочка ✓)
7. **Redeploy** Parsing Server

---

## ✅ После исправления:

В логах должно появиться:
```
INFO: Database initialized
INFO: Started server process [1]
INFO: Application startup complete.
```

---

## 🔍 Проверка:

1. **Parsing Server** → **Logs**
2. Не должно быть ошибок про подключение к базе данных
3. Должно быть сообщение `Database initialized`

---

## ⚠️ Важно:

- **DATABASE_PUBLIC_URL** работает из любого места (внутри и вне Railway)
- **DATABASE_URL** использует внутренний хост и может не работать между сервисами
- **Рекомендуется** использовать `DATABASE_PUBLIC_URL` для надежности

---

## 🎯 Итого:

1. ✅ Скопируйте `DATABASE_PUBLIC_URL` из Postgres → Variables
2. ✅ Вставьте в Parsing Server → Variables → `DATABASE_URL`
3. ✅ Сохраните и Redeploy

После этого подключение должно работать!


