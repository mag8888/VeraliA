# Проверка PARSING_SERVER_URL

## ❌ Проблема:

В логах видно, что запрос идет на неправильный URL:
```
Отправка запроса на парсинг: https://veralia-production.up.railway.app/api/analyze
```

Это URL **Client Server**, а не **Parsing Server**!

---

## ✅ Решение:

### Шаг 1: Найдите публичный URL Parsing Server

1. **Railway Dashboard** → Найдите сервис **Parsing Server**
2. Перейдите в **Settings** → **Networking**
3. Найдите **Public Domain** или **Generate Domain**
4. Скопируйте публичный URL (например: `https://parsing-server-production.up.railway.app`)

### Шаг 2: Обновите PARSING_SERVER_URL в Client Server

1. **Client Server** → **Settings** → **Variables**
2. Найдите переменную `PARSING_SERVER_URL`
3. Установите правильное значение:
   - **Вариант 1**: Прямой URL: `https://parsing-server-production.up.railway.app`
   - **Вариант 2**: Если сервисы в одном проекте: `${{ParsingServer.RAILWAY_PUBLIC_DOMAIN}}`
4. **Важно**: URL должен быть **публичным** (не внутренним `postgres.railway.internal`)
5. **Сохраните** изменения

### Шаг 3: Redeploy Client Server

1. После изменения переменной окружения
2. Railway автоматически перезапустит сервис
3. Или нажмите **Redeploy** вручную

---

## 🔍 Проверка:

### Проверка доступности Parsing Server:

Откройте в браузере:
```
https://ваш-parsing-server-url.up.railway.app/health
```

Должен вернуться:
```json
{"status":"healthy"}
```

### Проверка в логах:

После исправления в логах Client Server должно быть:
```
Отправка запроса на парсинг: https://parsing-server-production.up.railway.app/api/analyze
```

**НЕ** должно быть:
```
Отправка запроса на парсинг: https://veralia-production.up.railway.app/api/analyze
```

---

## ⚠️ Частые ошибки:

1. **PARSING_SERVER_URL указывает на Client Server** → Исправьте на URL Parsing Server
2. **Используется внутренний URL** → Используйте публичный URL
3. **URL без протокола** → Код автоматически добавит `https://`, но лучше указать явно
4. **Parsing Server не имеет публичного домена** → Сгенерируйте домен в Settings → Networking

---

## 📋 Чек-лист:

- [ ] Parsing Server имеет публичный домен
- [ ] PARSING_SERVER_URL правильный в Client Server
- [ ] URL начинается с `https://`
- [ ] URL указывает на Parsing Server, а не Client Server
- [ ] Client Server перезапущен после изменений
- [ ] Проверен `/health` endpoint Parsing Server

---

## ✅ После исправления:

1. **Redeploy** Client Server
2. Проверьте логи Client Server
3. Попробуйте загрузить скриншот снова
4. Запрос должен идти на правильный URL

---

## 🎯 Итого:

**Проблема**: `PARSING_SERVER_URL` указывает на Client Server вместо Parsing Server.

**Решение**: Обновите `PARSING_SERVER_URL` на публичный URL Parsing Server.

